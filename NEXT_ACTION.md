# NEXT_ACTION — AFDX 지터 측정·이상검출 (2026-09-10 기준)

## 핵심 결정 (먼저)
**"정상 BAG 측정 반복" vs "의도적 고장 주입으로 검출 검증" 중 → 고장 주입이 압도적으로 가치 높음. (이번 세션에 실행 완료, 6/6 PASS)**

근거:
- 정상 BAG 측정은 이미 포화(BAG 1~16ms 스위프, 다중 VL, 프레임 크기, best-of-N 다 완료). 지터 절대값은 **호스트 노이즈 지배(동일조건 재측정 2~30µs 편차)** 라 더 반복해도 새 정보 없음.
- 이상 검출("이상 검출" = 과제 절반)은 그동안 Lmax·정상만 검증. 다양한 실제 고장 검출은 **미검증**이었음.
- 고장 주입은 **프레임레벨(SN/Lmax/VLID) 판정이라 호스트 노이즈에 강건** → 이 PC에서도 신뢰성 있게 검증 가능.
- 실제로 고장 주입이 **손실 검출 맹점(실제 버그)을 발견·수정**시킴 → 연구/개발 가치 실증.

앞으로도: **정상 재측정 < 고장 검출 커버리지 확장.**

---

## 1. 현재 확정된 결과 (A: 확실히 말할 수 있는 것)
근거 데이터: `docs/measurement_series.md`, `results/`, `results/fault_injection.json`.

- **평균 packet interval이 BAG를 정확히 추종** — 모든 BAG(1/2/4/8/16ms)에서 |평균ΔT−BAG| ≤ 약 0.8µs. 호스트 부하·런 무관하게 견고. → FPGA의 BAG 페이싱(대역폭 보장)이 정확함.
- **VL별 pacing 유지** — 단일 VL에서 평균 = BAG.
- **다중 VL(3 VL 동시)에서도 각 VL이 BAG 유지** — VL1/2/3 각각 평균=BAG, 손실 0 (AFDX per-VL 격리 실증).
- **프레임 크기(60~1514B) 변화에도 페이싱 유지** — 평균=BAG.
- **이상 검출이 실제 고장을 재현성 있게 검출** — Lmax초과/Rate위반/시퀀스손실/잘못된VLID 6/6 PASS. 프레임레벨 판정이라 호스트 노이즈에 강건.

## 2. 현재 측정의 한계 (B: 아직 말하면 안 되는 것)
- **"FPGA intrinsic jitter = X µs"** ❌ — 측정한 건 외부 수신 패킷 지터(SW 타임스탬프)이고, 여기엔 FPGA + ABM + PC 커널/스케줄러/C-state 노이즈가 다 섞여 있음. 동일조건 재측정에도 2~30µs 편차. FPGA 내부 지터를 분리할 수 없음.
- **"FPGA 내부 scheduler jitter = X µs"** ❌ — 내부 단계별 타임스탬프(Timestamp A: scheduler release)가 없음. 외부에서 관측 불가.
- **"MAC TX hardware jitter = X µs"** ❌ — MAC 송출 시점 타임스탬프(Timestamp B)가 없음.
- 말할 수 있는 최대치: **"외부 관측 패킷 지터(SW ts, 호스트노이즈 포함)는 조용한 관측에서 단일 µs 수준"** = FPGA 지터의 느슨한 측정 상한. 정밀 확정은 불가.

**왜**: 지터는 여러 노이즈원의 합(FPGA + ABM 통과 + PC 측정계). PC 측정계 노이즈가 FPGA 지터와 같은 자릿수(µs)라 분리 불가. 분리하려면 **측정 지점을 MAC/PHY에 가깝게** 옮기거나(HW timestamp) **FPGA 내부에 타임스탬프**를 넣어야 함.

## 3. 추가로 해볼 가치가 있는 시험 (item3 결론)
PC 노이즈 민감도는 이미 정량화됨(C-state ON 중앙3.2/범위1.8~12µs, OFF 중앙6.4/범위2.5~30µs). 결론: **측정계가 부하에 민감함을 정량 확인** = 목적 달성. 추가로 가치 있는 것:
- **고장 검출 커버리지 확장** (아래 우선순위 1) — 가치 높음.
- (선택) 장시간 연속 측정으로 드리프트/희귀 고장 관찰 — 가치 중간.

## 4. 굳이 하지 않아도 되는 시험
- **정상 BAG 측정 추가 반복** — 포화됨. 지터 절대값은 노이즈 한계라 더 반복해도 무의미.
- **CPU affinity / IRQ affinity / governor / RT priority 개별 튜닝** — 이미 `measure_setup.sh`에 통합. C-state가 지배 요인임을 확인했고, 더 낮추려는 시도는 "숫자 억지로 낮추기"라 연구가치 없음(목적은 정량화지 최소화 아님).
- **CPM/ABM 접근 재시도** — ABM은 단방향 탭, CPM 경로 없음 확정. 재개 조건 충족 전엔 무의미.

## 5. 안전한 이상 주입 시험 계획 (완료)
`fault_injection_test.py` — 정상→주입→검출→복구, 6/6 PASS. **안전 원칙 준수**(kfdx_app 정상 명령 --add/--set/--send + pcap 조작만, 레지스터 write·`--link_down` 미사용).
- S0 정상 → NORMAL
- S1 Lmax초과(1514B, 정책 Lmax=512) → FAULT(Lmax)
- S2 Rate위반(VL bag=50=500µs, 정책 BAG=2000µs) → WARNING/FAULT(Rate)
- S3 시퀀스손실(정상 캡처에서 8프레임 삭제=drop 재현) → FAULT(손실)
- S4 잘못된VLID(허용={1}인데 VL2 송신) → FAULT(허용외 VLID)
- S5 복구 → NORMAL
- **부수 성과**: 손실 검출 맹점(시간게이트 오분류) 발견·수정.

## 6. 논문/연구로 이어질 수 있는 부분 (item5 A~E)
| 항목 | 현재 결과만으로 | 추가 필요 |
|---|---|---|
| **A. AFDX VL/BAG/Lmax 모니터링 개발** | ✅ 가능(완료) — 구성/송신/카운터/판정/리포트 | — |
| **B. AFDX traffic anomaly detection** | ✅ 가능(완료) — 5종 고장 검출 6/6 재현 검증 | — |
| **C. 다중 VL timing isolation 검증** | ✅ 가능(완료) — 3 VL 경쟁에도 per-VL BAG 유지 | 더 강한 경쟁(작은 BAG·많은 VL)로 한계점 탐색은 선택 |
| **D. fault localization** | △ 부분 — "어느 VL/무슨 종류"까지는 됨. "FPGA 파이프라인 어느 단계"는 불가 | **FPGA 내부 Timestamp A/B** 필요 |
| **E. FPGA 내부 timestamp 기능 추가 연구** | ❌ 현재 불가 | **FPGA RTL 수정**(scheduler release TS + MAC TX TS) + 드라이버 노출 |

→ **현재 결과만으로 논문화 가능한 축: A(모니터링 툴) + B(anomaly detection 재현검증) + C(timing isolation).** D/E는 FPGA RTL 작업이 전제.

## 7. 다음 작업 우선순위 (최대 3)

### 우선순위 1 — 이상 검출 커버리지·강건성 확장
- **목적**: 검출기가 더 다양한 실제 고장(버스트, 중복, 재정렬, 다중 동시 고장, 경계 케이스)에도 재현성 있게 동작함을 검증·강화.
- **왜 필요**: 이번에 고장주입이 실제 버그(손실 맹점)를 잡음. 검출기의 신뢰성이 이 툴의 핵심 가치이고, 프레임레벨이라 **호스트 노이즈와 무관하게 이 PC에서 신뢰성 있게** 더 밀어붙일 수 있음. 논문 축 B를 강화.
- **실행**: `fault_injection_test.py`에 S6 버스트(bag=1), S7 중복(pcap에 프레임 복제), S8 재정렬(pcap 순서 섞기), S9 다중동시(Lmax+손실) 추가. 각 기대 판정 검증.
- **성공 기준**: 추가 시나리오 전부 PASS, 또는 실패 시 원인이 실제 검출기 결함으로 특정되고 수정됨.
- **실패 시 다음**: 검출 로직의 한계(예: 재정렬과 손실 구분 불가)를 문서화하고, 필요한 추가 정보(예: 정확한 SN/타임스탬프)를 명세 → D/E로 연결.

### 우선순위 2 — 라이브 스트리밍 모니터
- **목적**: 일회성 캡처가 아니라 **연속 캡처 + 롤링 이상검출 + GUI 실시간 판정**으로 상시 모니터링 툴화.
- **왜 필요**: 과제 제목이 "모니터링"이고 데모 툴 목적. 논문 축 A(모니터링 개발 결과)를 완성. 실제 배치 가능한 형태.
- **실행**: `serial_bridge.py`에 스트리밍 캡처 op(tcpdump 파이프 → 롤링 윈도우로 detect_anomalies) 추가, `kfdx.html`에 실시간 판정/카운터/미니차트 대시보드.
- **성공 기준**: 브라우저에서 실시간으로 프레임/판정/이상카운트가 갱신되고, 고장주입 시 즉시 FAULT로 바뀜.
- **실패 시 다음**: 실시간 처리 부하가 크면 주기적 폴링(1s 배치)로 낮춤.

### 우선순위 3 — (조건부) FPGA 내부 Timestamp 명세 초안
- **목적**: D/E(fault localization, 내부 지터)로 가기 위한 **FPGA RTL 요구사항 명세** 작성 (Timestamp A=scheduler release, B=MAC TX, 드라이버 노출 방식).
- **왜 필요**: PC 방법의 한계(내부 지터 분리 불가)를 넘는 유일한 길. 지금 실측은 못 해도 **무엇을 넣어야 하는지 명세**는 현재 지식으로 작성 가능 → 다음 단계(HW팀) 전달용.
- **실행**: 과제 원문(6·7절)의 Timestamp A/B 정의 + 현재 KFDX 레지스터맵(cdma/user base) 조사 결과를 근거로, 필요한 카운터/레지스터/ioctl 초안 문서 작성.
- **성공 기준**: HW팀이 바로 검토 가능한 수준의 명세 초안(TS 위치·폭·읽기 인터페이스).
- **실패 시 다음**: 기존 aero_eth_app의 `--hwts` PTP 경로 재분석으로 유사 기능 재활용 가능성 탐색.

---
## 재개 조건 (CPM/ABM, 현재 중단)
아래 중 하나 생길 때만 정밀 지터(HW timestamp) 재개:
- CPM 직접 콘솔 케이블 확보 (진짜 콘솔 포트)
- CPM 실제 management port/IP 확인
- ABM bridge/configuration 방법 확보
- AeroRing/CPM 매뉴얼 확보
